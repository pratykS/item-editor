import React from "react";
import { Editor } from "../Editor/Editor";

const HomeComponent = () => {
  return (
    <div>
      <Editor></Editor>
    </div>
  );
};

export const Home = HomeComponent;
